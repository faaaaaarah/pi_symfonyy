<form method="post" action="{{ path('app_interventions_delete', {'id_int': intervention.idInt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intervention.idInt) }}">
    <button class="btn">Delete</button>
</form>
